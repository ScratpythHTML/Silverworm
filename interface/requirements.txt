# Silverworm — Python dependencies
# Install once (system-wide, CM5 model):
#   pip install -r interface/requirements.txt
# On modern Debian/Ubuntu you may need:
#   pip install -r interface/requirements.txt --break-system-packages
# Hardware transports (Linux only):
#   pip install -r interface/requirements-rpi.txt

# GUI
PyQt6>=6.4.0
PyQt6-Qt6>=6.4.0
PyQt6-sip>=13.4.0

# Computer vision + numerics
opencv-python>=4.5.0
numpy>=1.20.0

# Testing
pytest>=7.4.0
pytest-qt>=4.2.0
pytest-mock>=3.11.0
