# Raspberry Pi hardware transports.
#
# These packages need Linux kernel interfaces. On macOS, spidev cannot build
# because linux/spi/spidev.h is not available.

smbus2>=0.6.1; sys_platform == "linux"
spidev>=3.8; sys_platform == "linux"
